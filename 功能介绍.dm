# Gantt Timeline — 临床试验时间表管理器 · 功能文档

## 概述

Gantt Timeline 是一款专为**临床试验项目管理**设计的时间表与甘特图生成工具。系统采用前后端分离的 Web 架构（React 18 + Express + SQLite），提供甘特图可视化编辑、模板复用、资源分配、多格式导出、文件管理等核心功能，帮助临床研究团队高效规划和管理试验项目的时间节点。

### 适用场景

- 临床试验方案时间线规划（如 SAP 开发、数据库锁定、中期分析等）
- 多项目并行管理，统一模板标准化
- 团队角色责任分配与人员调度
- 向监管机构提交的时间表导出（PDF / Excel）

---

## 系统路由结构

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | 项目列表 | 首页，展示所有项目 |
| `/project/:id/gantt` | 甘特图编辑器 | 核心编辑页面 |
| `/project/:id/table` | 表格视图 | 任务列表表格展示 |
| `/templates` | 模板库 | 模板管理与编辑 |
| `/resources` | 资源管理 | 角色与人员管理 |

---

## 功能模块详解

### 1. 项目管理

**页面路径：** `/` （首页 → 项目列表）

#### 1.1 创建项目

点击「+ 新建项目」展开创建表单，填写以下字段：

| 字段 | 必填 | 说明 | 示例 |
|------|------|------|------|
| 项目名称 | ✅ | 项目唯一标识名称 | IMM2510-003 TNBC 项目 |
| 研究编号 | 否 | 临床试验编号 | IMM2510-003 |
| 适应症 | 否 | 疾病适应症 | TNBC（三阴乳腺癌） |
| 项目描述 | 否 | 多行文本备注 | 多中心、随机、双盲... |

创建成功后自动跳转至该项目甘特图页面，项目的起止日期由任务自动计算。

#### 1.2 项目列表

以表格形式展示所有项目：

| 列名 | 说明 |
|------|------|
| 名称 | 项目名称（加粗显示） |
| 研究编号 | `study_id`，无数据显示 `-` |
| 适应症 | `indication`，无数据显示 `-` |
| 开始日期 | 有任务时显示最早任务日期，无任务显示 `NA` |
| 操作 | 删除按钮 |

**交互：**
- 点击任意项目行 → 进入该项目的甘特图视图
- 点击删除按钮 → 确认后级联删除项目及其所有任务、依赖关系

#### 1.3 空状态

无项目时展示引导提示「暂无项目」，提供快速创建入口。

---

### 2. 甘特图编辑器（核心功能）

**页面路径：** `/project/:id/gantt`

这是系统的核心功能页面，以可视化甘特图方式管理任务时间线。页面由上至下分为六个区域：

---

#### 2.1 项目信息栏（顶部）

展示项目概要信息：
- **项目名称**（加粗）
- **研究编号**
- **适应症**
- **总体起止日期**（由任务自动计算，无任务时显示 `NA`）

> 点击项目列表返回按钮可回到首页。

---

#### 2.2 总工期行

从首项任务到末项任务的天数汇总。

- **显示内容**：起始日期 → 结束日期，总天数
- **自定义名称**：点击「总工期」标签可编辑为任意名称（如「项目总时长」），名称按项目存储在浏览器 localStorage 中
- **导出联动**：导出 PDF / Excel 时，自定义名称会被带入

---

#### 2.3 备注行

项目级别的备注说明栏，位于甘特图上方。

- **输入**：自由文本输入框，placeholder 为「添加项目备注说明...」
- **自动保存**：输入后 500ms 防抖自动保存到数据库，无需手动提交

---

#### 2.4 工具栏

左侧按钮组：

| 按钮 | 功能 | 说明 |
|------|------|------|
| **+ 新建** | 弹出新建对话框 | 选择类型（阶段 / 任务），填写信息后创建 |
| **▲** | 选中行上移 | 在同级任务/阶段中向上移动一位 |
| **▼** | 选中行下移 | 在同级任务/阶段中向下移动一位 |
| **🗑** | 删除选中任务 | 快捷键 `Delete` 等价操作 |
| **今天** | 定位到今天 | 滚动甘特图时间轴至当前日期 |

右侧按钮组：

| 按钮 | 功能 | 说明 |
|------|------|------|
| **📄 PDF** | 导出 PDF | 弹出选项后导出 |
| **📊 Excel** | 导出 Excel | 弹出选项后导出 |
| **↻** | 刷新 | 重新加载项目全部数据 |
| **📥 导入模板** | 从模板库导入 | 选择已发布模板，以锚定日期导入 |
| **💾 保存为模板** | 保存当前结构为模板 | 将项目任务结构保存到模板库 |

---

#### 2.5 新建任务/阶段对话框

点击「+ 新建」弹出模态对话框：

**类型切换：**
- 阶段（橙色标签）：顶层里程碑节点，无父级归属
- 任务（蓝色标签）：可选归属到某个阶段

**表单字段：**

| 字段 | 说明 |
|------|------|
| 类型 | 阶段 / 任务 切换按钮 |
| 归到阶段 | （仅任务）下拉选择归属的阶段 |
| 名称 * | 必填，阶段或任务名称 |
| 开始日期 | date picker |
| 结束日期 | date picker |
| 工期 | 自动计算（`(结束日期 - 开始日期) / 86400000 + 1`），也可手动修改后联动结束日期 |

**交互逻辑：**
- 切换类型时，阶段自动清空归属，任务自动选中第一个可用阶段
- 按 `Enter` 键直接创建
- 任务必须选择归属阶段才能创建（如无可选阶段则禁用创建）
- 创建成功后对话框关闭，甘特图自动刷新

---

#### 2.6 甘特图图表区域

##### 表格列定义

| 列名 | 宽度 | 说明 |
|------|------|------|
| 任务名称 | 360px | 树形结构，阶段加粗橙色，子任务蓝色缩进 |
| 开始日期 | 110px | 居中显示 |
| 结束日期 | 110px | 居中显示 `real_end`（实际结束日期） |
| 工期 | 65px | 自动计算 `end_date - start_date` 天数 |
| 负责角色 | 120px | 可点击按钮弹出多选菜单 |
| 负责人 | 120px | 可点击按钮弹出多选菜单（按角色过滤） |

##### 时间轴刻度

双层刻度：
- 上层：**月**（格式：`2025年 8月`）
- 下层：**日**（格式：`1`, `2`, `3`...）

##### 甘特条样式

- **阶段**：橙色 (`#d97706`) 加粗
- **子任务**：蓝色 (`#2563eb`)
- **独立任务**（无归属）：紫色 (`#8b5cf6`)

---

#### 2.7 甘特图交互

##### 拖拽调整
- 直接在甘特图上**拖拽任务条**可修改起止日期
- 拖拽结束后系统自动更新 `start_date`、`end_date`、`duration_days`
- 禁止调整进度条和任务条大小（仅允许平移拖拽）

##### 阶段联动拖拽
- 拖拽**阶段（里程碑）**条时，其下所有子任务**同步偏移**相同天数
- 偏移量 = 新区间首日子任务最早日期与拖拽前首日子任务最早日期的差值

##### 子任务拖拽
- 拖拽子任务条后，后端自动重新计算该阶段下后续兄弟任务的排序偏移

##### 双击编辑
双击任务条打开 [TaskEditModal](client/src/components/TaskEditor/TaskEditModal.jsx) 编辑弹窗，可修改：
- 任务名称
- 类型（阶段/任务，只读展示）
- 开始 / 结束日期
- 工期
- 备注

##### 依赖连线
- 在甘特条之间拖拽创建 **FS（完成-开始）依赖关系线**
- 点击连线可选中，按 `Delete` 删除
- 依赖关系存储到数据库，支持持久化

##### 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Delete` | 删除当前选中任务（需确认） |

##### 自动滚动
拖拽任务靠近甘特图边缘时自动滚动，速度 80px/tick。

---

#### 2.8 资源分配

##### 负责角色选择
- 点击表格列中的**角色按钮**（橙色），弹出多选菜单
- 角色列表来自资源管理模块
- 支持多选（checkbox），勾选/取消即时更新到数据库
- 无角色时显示提示「暂无角色，请先在资源管理中添加」

##### 负责人选择
- 点击表格列中的**人员按钮**（蓝色），弹出多选菜单
- 人员列表**按已选角色过滤**：只显示所选角色关联的人员
- 未选角色时显示所有人员
- 所选角色下无人员时提示「所选角色暂无人员」

##### 角色-人员关联机制
- 在资源管理中，每个角色绑定一个或多个人员姓名
- 甘特图中选择角色后，人员选择器自动过滤为该角色下的人员

---

#### 2.9 导出对话框

点击 PDF 或 Excel 按钮弹出选项：

| 选项 | 说明 |
|------|------|
| 包含甘特图 | 勾选后在表格下方附加甘特图横向条形图 |

- 导出 URL 携带 `label`（总工期自定义名称）和 `gantt`（是否含甘特图）参数
- 新标签页打开下载

---

#### 2.10 错误与加载状态

| 状态 | 表现 |
|------|------|
| **加载中** | 半透明白色遮罩 + 脉冲文字「加载中...」 |
| **加载失败** | 红色错误信息 +「重试」按钮 +「← 返回」按钮 |
| **空数据** | 正常展示空甘特图，无特殊提示 |

---

### 3. 表格视图

**页面路径：** `/project/:id/table`

以**搜索表格**形式列出项目所有任务，适合快速浏览和数据检查。

#### 3.1 顶部工具栏

- **← Gantt View**：切换回甘特图视图
- **搜索框**：按任务名称关键字实时过滤（大小写不敏感）
- **阶段筛选下拉**：按阶段过滤，默认「All Phases」
- **任务计数**：右侧显示当前过滤结果数量

#### 3.2 表格列

| 列名 | 说明 |
|------|------|
| **WBS** | 工作分解结构编码（`wbs_code`，无编码时回退为任务 ID） |
| **名称** | 子任务缩进显示 `↳` 前缀，阶段显示 `◆` 标记 |
| **阶段** | 关联阶段名称 + 颜色圆点 |
| **开始日期** | `start_date` |
| **结束日期** | `end_date` |
| **工期** | `duration_days` + `d` 后缀 |
| **进度** | 进度条（蓝色填充）+ 百分比数字 |

#### 3.3 交互

- 点击任意任务行 → 切换回甘特图视图
- 搜索和筛选实时生效，无需回车

---

### 4. 模板库

**页面路径：** `/templates`

模板库用于创建和管理可复用的项目任务模板，实现项目标准化。

#### 4.1 模板列表页

- **卡片展示**：每个模板一张卡片，网格布局（2 列）
- **卡片信息**：
  - 名称 + 状态标签（✅ 已保存 / 📝 草稿）
  - 描述（如有）
  - 阶段数 · 子任务数 · 总工期
- **操作**：
  - ✏️ 编辑：进入模板编辑器
  - 删除：确认后删除模板及其所有任务

#### 4.2 创建模板对话框

两种创建方式：

| 方式 | 说明 |
|------|------|
| **空白模板** | 下拉选择「空白模板（手动编辑）」，手动添加阶段和任务 |
| **从项目导入** | 下拉选择已有项目，将项目的任务结构导入为新模板 |

必填字段：模板名称

#### 4.3 模板编辑器

点击编辑进入模板详情页面，是模板库的核心功能区域。

##### 顶部信息栏
- **返回按钮**：「← 返回」回到模板列表
- **模板名称**：点击可编辑（inline editing）
- **统计信息**：× 阶段 · × 子任务
- **删除按钮**

##### 备注行
- 自由文本输入，失焦时自动保存

##### 添加阶段/任务表单

| 字段 | 说明 |
|------|------|
| 类型切换 | 阶段 / 任务 |
| 归属阶段 | （仅任务）下拉选择，可选「无归属（独立任务）」 |
| 名称 | 文本输入，按 Enter 快速添加 |
| 工期（天） | 数字输入 |

- 添加后**自动排程**：阶段并行，子任务按顺序累积排列

##### 时间线预览
- 横向条形图预览所有任务时间排布
- 天数列刻度（每 5 天标数字）
- 阶段：琥珀色渐变条，显示工期
- 子任务：蓝色圆角条，显示工期
- 独立任务：紫色条
- 右侧显示负责角色

##### 任务清单（可编辑表格）

| 功能 | 操作方式 |
|------|----------|
| ▲▼ 上下排序 | 点击每行左侧的上下箭头 |
| 编辑名称 | 点击名称文字进入 inline 编辑 |
| 编辑工期 | 点击工期数字进入 inline 编辑 |
| 移动归属 | 悬停出现下拉菜单，切换归属阶段 |
| 类型转换 | 点击 🔄 在阶段与任务之间互转 |
| 删除任务 | 点击 ✕ |
| 分配角色 | 点击角色区弹出多选 checkbox |

##### 保存操作

| 按钮 | 行为 |
|------|------|
| **取消** | 退出编辑器，回到模板列表 |
| **保存为草稿** | `status = 'draft'`，草稿不可被项目导入 |
| **保存** | `status = 'published'`，发布后可在甘特图中导入使用 |

- 底部状态栏显示当前发布状态

#### 4.4 模板导入流程

在项目甘特图页面点击「📥 导入模板」：
1. 弹窗列出所有**已发布**模板
2. 单选一个模板
3. 设置**锚定日期**：模板的第一个任务以此日期为基准，其余任务按相对偏移量自动计算日期
4. 点击导入，模板任务自动创建到当前项目

---

### 5. 资源管理

**页面路径：** `/resources`

管理系统中的角色和人员信息，为甘特图任务分配提供数据源。

#### 5.1 新建资源

| 字段 | 必填 | 说明 |
|------|------|------|
| 角色 * | ✅ | 角色名称，如「临床监查员」「数据管理员」「统计分析师」 |
| 颜色 | 否 | color picker，标记角色颜色 |
| 负责人 * | ✅ | 至少添加一个负责人姓名，可添加多个（tag 模式） |

- 添加人员：输入姓名后按 + 按钮或 Enter，生成蓝色 tag
- 删除人员：点击 tag 上的 ×

#### 5.2 资源列表表格

| 列名 | 说明 |
|------|------|
| 角色 | 颜色圆点 + 角色名称，**支持按角色名排序** |
| 负责人 | 蓝色 tag 列表，**支持按人员数量排序**，双击 tag 进入编辑 |
| 操作 | 删除按钮 |

**排序交互：**
- 点击列头切换排序（升序 / 降序 / 取消）
- 当前排序列显示 ▲ 或 ▼ 指示器

**内联编辑：**
- 双击负责人 tag → 进入编辑模式
- 可添加 / 删除人员 tag
- 点击「保存」提交，点击「×」取消

#### 5.3 与甘特图的联动

- 资源角色自动出现在甘特图**负责角色**选择器中
- 资源角色下的人员自动出现在甘特图**负责人**选择器中（按角色过滤）

---

### 6. 文件管理

系统内置项目文件管理功能（通过 API 调用，前端可集成上传组件）。

#### API 端点

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/projects/:id/files` | 列出项目所有文件 |
| POST | `/api/projects/:id/files` | 上传文件（base64 JSON body） |
| GET | `/api/files/:fileId` | 下载文件 |
| DELETE | `/api/files/:fileId` | 删除文件 |

- 单个文件大小限制：**10MB**
- 数据以 base64 编码存储在 SQLite 中

---

### 7. 系统设置

通用的键值对设置存储，支持前端按需读写。

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/settings` | 获取所有设置（JSON 对象） |
| POST | `/api/settings` | 保存单个设置（`{ key, value }`） |

- 值自动序列化/反序列化（JSON 类型自动处理）
- 使用 SQLite `INSERT OR REPLACE` 保证幂等

---

### 8. 导出功能

#### 8.1 PDF 导出

- 使用 PDFKit 生成
- 自动检测并注册中文字体（`SimHei` 黑体）
- 支持**纵向**（仅表格）和**横向**（含甘特图）两种布局
- 包含内容：
  - 项目名称标题 + 导出时间
  - 总工期汇总行（使用自定义名称）
  - 树形排序任务表
  - 可选甘特图条形图（月标头 + 日刻度 + 任务条 + 工期标注）

#### 8.2 Excel 导出

- 使用 ExcelJS 生成
- 包含两个工作表：
  1. **项目任务**：标题行 + 总工期汇总行 + 任务列表（含样式、边框、缩进）
  2. **甘特图**（可选）：月头、日刻度、任务条形图（单元格填充模拟）

---

### 9. 国际化 (i18n)

系统基于 **react-i18next** 实现多语言支持。

| 语言 | 命名空间 |
|------|----------|
| English (en) | `common.json`, `gantt.json` |

- 语言文件位于 [client/src/i18n/locales/](client/src/i18n/locales/)
- 中文文本当前硬编码在组件中，可根据需要提取为 `zh` 语言包

---

## 技术架构

| 层级 | 技术选型 | 版本/说明 |
|------|----------|-----------|
| 前端框架 | React 18 | 函数组件 + Hooks |
| 路由 | React Router v6 | 5 个页面路由 + catch-all 重定向 |
| 甘特图组件 | dhtmlx-gantt | 专业甘特图库 |
| 样式方案 | Tailwind CSS | 原子化 CSS |
| HTTP 客户端 | Axios | 统一错误拦截 |
| 后端框架 | Express.js | `express-async-errors` 异步错误处理 |
| 数据库 | SQLite | `sql.js`（WASM 实现，纯 JS，无需编译） |
| 构建工具 | Vite | 前端开发服务器 + 生产构建 |
| 国际化 | react-i18next | 命名空间翻译 |
| PDF 导出 | PDFKit | 服务端生成 |
| Excel 导出 | ExcelJS | 服务端生成 |

### 数据库表结构（核心）

| 表名 | 用途 |
|------|------|
| `projects` | 项目信息 |
| `tasks` | 任务/阶段，支持父子关系、排序、WBS、资源分配 |
| `dependencies` | 任务间 FS 依赖关系 |
| `phases` | 阶段定义（颜色、名称） |
| `templates` | 模板定义（名称、状态、来源） |
| `template_tasks` | 模板内任务（相对偏移量） |
| `resources` | 资源（角色、颜色、成员列表） |
| `project_files` | 项目附件（base64 存储） |
| `settings` | 系统设置（键值对） |

---

## 环境部署与迁移

### 前置条件

- **Node.js 18+**（唯一外部依赖，`node -v` 确认）
- 无需 Python、C++ 编译器、node-gyp（数据库使用 WASM 实现）

### 首次安装

```bash
# 一键安装根层 + server + client 三层依赖
npm run setup
```

### 开发模式

```bash
npm run dev
```

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端 | `http://localhost:5173` | Vite 开发服务器，热更新，自动代理 API 到 3001 |
| 后端 | `http://localhost:3001` | Express API 服务器，文件变动自动重启 |

### 生产部署

```bash
npm run build   # 前端构建到 client/dist/
npm start       # Express 同时提供静态文件 + API，单端口 3001
```

访问 `http://localhost:3001` 即可。

### 迁移到另一台电脑

项目不含原生编译依赖，迁移极为简单：

```bash
# 1. 打包（排除 node_modules，可选保留 data/）
#    直接用 zip/7z 压缩整个项目文件夹即可

# 2. 解压到目标电脑，进入目录
cd gantt-timeline

# 3. 安装依赖
npm run setup

# 4. 启动
npm run dev      # 开发
# 或
npm run build && npm start   # 生产
```

#### 关于数据迁移

| 场景 | 处理方式 |
|------|----------|
| 保留现有数据 | 打包时保留 `data/gantt.db`，复制到新电脑同一位置 |
| 全新开始 | 不打包 `data/` 目录，启动后 schema 和数据库自动创建 |
| 保留种子数据 | 打包 `data/` 目录，或在新电脑上运行 `npm run seed` |

#### 注意事项

- **`sql.js`**：SQLite 的 WASM 编译版本，纯 JavaScript 实现，跨平台无需编译
- **`dhtmlx-gantt`**（v9）：商业甘特图库，新机器上需确认 License 仍然有效
- **`puppeteer`**：在 `optionalDependencies` 中，安装失败不影响项目运行（当前未使用）
- **`exceljs`**：在 `optionalDependencies` 中，若安装失败则 Excel 导出功能不可用
- **PDF 中文字体**：PDF 导出硬编码了 Windows 字体路径 `C:/Windows/Fonts/simhei.ttf`，非 Windows 或缺少该字体时中文会显示异常

### 种子数据

```bash
npm run seed
```

根据 `server/db/seed.js` 内容初始化示例项目数据。

---

## 数据流概览

```
用户操作 → React 组件 → Axios API 调用 → Express 路由 → sql.js (WASM) → SQLite
                                              ↓
                               PDFKit / ExcelJS → 下载流响应
```

- **前端**：通过 `useGantt` Hook 封装 dhtmlx-gantt 初始化与事件绑定
- **数据加载**：`getProject()` + `getGanttData()` + `getResources()` 三路并发
- **更新保护**：`updatingRef` 防止 `onAfterTaskUpdate` 在程序化 `loadAll()` 期间触发
- **阶段日期派生**：里程碑的起止日期由其子任务动态计算（`deriveMilestoneDates`）

---

> 文档版本: V3.1 · 最后更新: 2026-08-11
